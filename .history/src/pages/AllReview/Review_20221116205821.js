import React from 'react';

const Review = ({review}) => {
    const {name,email,message,address,reviewTitle} = review
    return (
        <div>
            
        </div>
    );
};

export default Review;